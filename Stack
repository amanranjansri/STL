#include <iostream>
#include <stack>
using namespace std;

int main()
{
    stack<string> s;

    s.push("Aman Ranjan");
    s.push("Alok Ranjan");
    s.push("Ashish Ranjan");
    s.push("Shubham Ranjan");

    cout << "Top element ->" << s.top() << endl;

    s.pop();

    cout << "Top element ->" << s.top() << endl;

    cout << "size of  element ->" << s.size() << endl;

    cout << "Empty or not ->" << s.empty();

    return 0;
}
